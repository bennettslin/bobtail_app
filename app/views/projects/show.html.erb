<%= render "sidebar" %>

<div id="main_content">
  <h1><%= @project.title %></h1>
  <div id="project_text">
    <%= @project.description.html_safe %>
  </div>

  <% if @admin == current_admin %>

  <div id="admin_span">
    <%= form_for @project do |f| %>
      <span><%= f.submit "Move up", name: "order_up" %>
      <%= f.submit "Move down", name: "order_down" %>
      Order number is <%= @project.order_num %>.<span>
    <% end %>
  </div>

  <div id="admin_span">
    <span><%= link_to "Update this project", edit_project_path(@project) %> |
      <%= link_to "Delete this project", project_path(@project),
      method: :delete, data: { confirm: "Are you sure?" } %></span>
  </div>
  <% end %>
</div>

